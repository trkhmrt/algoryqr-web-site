# Local test: next dev çalışırken varsayılan AUTH_BASE zaten http://localhost:8099.
# Farklı port için .env.local ile override edin:

# AUTH_BASE=http://localhost:8080

# Gateway (isteğe bağlı)
# GATEWAY_BASE=http://localhost:8xxx
